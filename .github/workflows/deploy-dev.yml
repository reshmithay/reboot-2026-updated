name: Deploy to Dev

on:
  push:
    branches: [develop]

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    environment: development

    steps:
      - uses: actions/checkout@v4

      - name: Deploy to dev environment
        run: |
          echo "Deploying to dev..."
          # Add your dev deployment steps here
          # e.g., fly.io, Railway, Render, or a dev K8s cluster
